# Copy to .env.local (never commit .env.local).
# Match the exact variable order/format used in `.env.local`.
# Dashboard is the secondary local app on port 5174.

VITE_CONVEX_URL=https://YOUR_DEPLOYMENT.convex.cloud
VITE_CLERK_PUBLISHABLE_KEY=pk_test_YOUR_INSTANCE.clerk.accounts.dev$

VITE_PORTAL_URL=http://localhost:5173
VITE_DASHBOARD_URL=http://localhost:5174
