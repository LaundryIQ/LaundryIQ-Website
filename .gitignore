node_modules/
.turbo/
dist/
.next/
.expo/
*.env.local
.env
.env.local

# Convex generated client/server bindings are consumed by other workspace apps,
# so they must be committed for remote builds like Vercel previews.
